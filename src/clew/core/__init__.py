"""clew.core — data models, storage, and trace semantics.

Subpackages and modules in this package are owned by the
``foundation`` (models), ``core-data`` (store, trace, format), and
``core-semantics`` (branch, replay, diff) tasks.
"""
